#include <iostream>
#include <vector>
#include <cmath>

struct BinaryTreeNode {
    int data;
    BinaryTreeNode* left;
    BinaryTreeNode* right;

    BinaryTreeNode(int x)
        : data(x), left(nullptr), right(nullptr)
        {

        }
};


bool HasPath(BinaryTreeNode* node, int sum) {
}


int main() {

    return 0;
}